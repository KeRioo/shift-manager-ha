name: Work Schedule
version: 1.0.5
slug: work_schedule
description: Shift/work schedule manager with quarterly calendar, undo support, and HA sensors
url: "https://github.com/KeRioo/shift-manager-ha"
startup: services
init: false
arch:
  - amd64
  - aarch64
image: "ghcr.io/kerioo/{arch}-addon-work-schedule"
ingress: true
ingress_port: 8000
panel_icon: "mdi:calendar-clock"
panel_title: "Work Schedule"
ports:
  8000/tcp: 8000
ports_description:
  8000/tcp: "Web UI and API"
map:
  - config:rw
options:
  mode: addon
schema:
  mode: "str"
